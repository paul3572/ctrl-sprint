# TaskPlanner Demo Application

## Pre-steps

```
npm install -g @angular/cli
ng new frontend --standalone --routing --style=css --skip-tests
ng serve

ng generate component home --standalone --skip-tests
```